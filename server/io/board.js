exports.io = function (ws, model, config) {
	var board = ws.ch.registerChannel('board');

	// session.on('c_boards', function (q) {
	// 	model.View.create(q);
	// });

	// session.on('r_boards', function (q) {
	// 	model.View.find(q);
	// });

	// session.on('u_boards', function (q) {
	// 	model.View.update(q);
	// });

	// session.on('d_boards', function (q) {
	// 	model.View.destroy(q);
	// });
};
