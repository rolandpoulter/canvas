'use strict';
// var browser_sync = require('browser-sync');
var gulp         = require('gulp');

gulp.task('browser_sync', ['build'], function () {
	// browser_sync.init(['build/**'], {
	// 	server: {
	// 		baseDir: 'build'
	// 	}
	// });
});
